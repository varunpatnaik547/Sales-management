"use client";
export function DataTable({rows, columns, redKey}:{rows:Record<string,unknown>[],columns:{key:string;label:string}[],redKey?:string}) { return <div className="overflow-x-auto rounded-lg border bg-white"><table className="w-full text-left text-sm"><thead className="bg-slate-50"><tr>{columns.map(c=><th className="p-3 font-medium" key={c.key}>{c.label}</th>)}</tr></thead><tbody>{rows.length?rows.map((row,i)=><tr className="border-t" key={String(row.id||i)}>{columns.map(c=>{const red=redKey===c.key&&Number(row.amount_claimed)>Number(row.approved_amount); return <td className={`p-3 ${red?"font-semibold text-red-600":""}`} key={c.key}>{String(row[c.key]??"—")}</td>})}</tr>):<tr><td colSpan={columns.length} className="p-8 text-center text-slate-500">No records found.</td></tr>}</tbody></table></div> }
